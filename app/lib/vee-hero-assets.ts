import { VISUAL_EDITING_PROMO_IMAGE } from '@/app/lib/visual-editing-promo-image'

/**
 * Hero art for Visual Experience Engine — local asset (Figma MCP URLs expire ~7 days).
 * Same “designer in yellow” preview used in the site header mega menu.
 */
export const veeHeroPrimaryArt = VISUAL_EDITING_PROMO_IMAGE
